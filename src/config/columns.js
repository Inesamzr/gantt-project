export const COLS = {
  id: 40,
  name: 300,
  assignee: 170,
  start: 150,
  duration: 70,
  actions: 80,
};

export const LEFT_WIDTH =
  COLS.name + COLS.assignee + COLS.start + COLS.duration + COLS.actions;
